#include <bits/stdc++.h>
using namespace std;

int main()
{
    int array[] = {5, 4, 3, 2, 1};
    int size = sizeof(array) / sizeof(array[0]);

    

    for (int i = 0; i < size; i++)
    {   
        cout<<"["<<array[i]<<"]";
    }
}